# Development Environment - Auto deploy for testing
name: Deploy to Development

on:
  push:
    branches: [develop]
  workflow_dispatch:

env:
  ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}

jobs:
  deploy-to-dev:
    runs-on: ubuntu-latest
    environment: development  # Separate environment
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Deploy to Development Environment
        run: |
          echo "Deploying to https://cinema-dev.azurecontainerinstances.io"
          # Deploy to development container instance
          
      # Add same build steps but deploy to dev environment